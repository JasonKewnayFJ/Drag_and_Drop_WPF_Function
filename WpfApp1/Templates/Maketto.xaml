<UserControl x:Class="WpfApp1.Templates.Maketto"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Templates"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Border Background="White" CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Label Content="{Binding Title}" Grid.Row="0" Foreground="Black"/>
                <TextBlock Text="{Binding Description}" Grid.Row="1" Foreground="Gray"/>
                <TextBlock Text="{Binding Id}" Grid.Row="2" Foreground="DarkGray"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
